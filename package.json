{
  "name": "teamflow-ai-root",
  "version": "1.2.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0 <23.0.0"
  },
  "scripts": {
    "build": "cd server && npm ci --include=dev && npm run build",
    "start": "cd server && node dist/server.js"
  }
}
